NODE_ENV=development
COOKIE_SECURE=false
PORT=3005

# Production (Namecheap + HTTPS) — do not use these values on localhost:
# NODE_ENV=production
# COOKIE_SECURE=true
# BIND_HOST=127.0.0.1
# ALLOWED_ORIGINS=https://YOUR_DOMAIN

TELEGRAM_API_ID=
TELEGRAM_API_HASH=
# TELEGRAM_ENABLED=false

AUTH_SMTP_HOST=
AUTH_SMTP_PORT=587
AUTH_SMTP_USER=
AUTH_SMTP_PASS=
AUTH_SMTP_FROM_NAME=Rabط

RABT_SECRET_KEY=
RABT_BACKUP_KEY=

# Public HTTPS origin for campaign tracking and inline email images. Never localhost or a LAN IP.
# PUBLIC_URL=https://YOUR_DOMAIN

SUPER_ADMIN_EMAIL=
SUPER_ADMIN_PASSWORD=
# SUPER_ADMIN_RESET=true
